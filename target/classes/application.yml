spring:
  datasource:
    url:
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  profiles:
    active: default
server:
  port: 8080

aliyun:
  oss:
    endpoint:
    accessKeyId:
    accessKeySecret:
    bucketName:


alipay:
  app-id:
  private-key:
  alipay-public-key:
  server-url:
  charset:
  sign-type:
  notify-url:
  return-url: